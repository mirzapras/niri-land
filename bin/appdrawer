#!/usr/bin/env bash
 
export WAYLAND_DISPLAY="${WAYLAND_DISPLAY:-wayland-1}"
export XDG_CURRENT_DESKTOP=niri
export XDG_SESSION_TYPE=wayland
 
exec rofi -show drun -config "$HOME/.config/rofi/appdrawer.rasi"
